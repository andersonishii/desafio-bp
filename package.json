{
  "scripts": {
    "cypress:open": "cypress open --config-file cypress.configDesafio.js",
    "cypress:run": "cypress run --config-file cypress.configDesafio.js",
    "cypress:api:open": "cypress open --config-file cypress.configDesafioAPI.js",
    "cypress:api:run": "cypress run --config-file cypress.configDesafioAPI.js"
  },
  "devDependencies": {
    "chai-json-schema": "^1.5.1",
    "cypress": "^13.6.6"
  }
}
